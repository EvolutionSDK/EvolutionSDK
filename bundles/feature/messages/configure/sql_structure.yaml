message:
	singular: message
	plural: messages
	fields:
		namespace: string
		message: string
		type: string
		field: string
		value: string
		status: "enum('active','cleared')"
		viewed: "enum('no','yes')"
	hasOne:
		- session.list
		- members.account